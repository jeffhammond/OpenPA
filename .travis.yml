sudo: false
language: c
os:
- linux
- osx
compiler:
- gcc
- clang
env:
  matrix:
  - CFLAGS=-std=c89
  - CFLAGS=-std=c99
  - CFLAGS=-std=c11
  - CFLAGS=-std=gnu89
  - CFLAGS=-std=gnu99
  - CFLAGS=-std=gnu11
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    # clang-3.8 comes from this
    - llvm-toolchain-precise
    packages:
    - gcc-4.9
    - gcc-5
    - gcc-6
    - clang-3.8
install:
- echo $PWD
- export PATH=$PWD/deps/bin:$PATH
- ls -alrt *
- sh $PWD/.travis-install-autotools.sh $PWD/deps
before_script:
- ./autogen.sh
script:
- ./configure
- make
- make check
after_failure:
- echo "Sad panda"
- find . -name config.log -exec grep -L "configure: exit 0" {} ";" | xargs cat
notifications:
  email:
    recipients:
    - jeff.science@gmail.com
    on_success:
    - change
    on_failure:
    - always
